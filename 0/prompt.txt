<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  Two supported event orderings can replace a current or successfully updated card with historical snapshot data. Both regressions were reproducible despite the focused suite, build, and typecheck passing.

Full review comments:

- [P1] Preserve results when originating toolInfo arrives late — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:763-765
  When an app action completes before a host that initially omitted `toolInfo` later publishes the originating card's identity, `hasDetachedAppResult` is true while `supersededHostIdentity` is null, so the first identity is treated as a card switch. The subsequent reset and restore replace the successful app result—for example, an approved result—with the historical originating card; retain or bind the detached result unless the late identity is provably a different card.

- [P1] Keep stale outer metadata from replacing current output — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:824-830
  When the same-origin inner and outer OpenAI globals are on different card revisions, current inner `toolOutput` can be followed by stale capability-only metadata from the outer target. This predicate treats any such capability after unkeyed data as a replacement result, allowing its restore to overwrite the current card with the stale snapshot; correlate metadata with its target/revision rather than assuming it belongs to the displayed output.
  </results>
  </user_action>