Stop hook feedback:
[go review see if you can simplify things and merge when all done and do e2e]: The condition requires three sequential actions: (1) go review and simplify, (2) merge when all done, (3) do e2e. Transcript evidence shows: (1) simplification review complete — agent states 'found nothing to cut', code is well-factored; (2) merged — PR #2298 squash commit 32ab462af confirmed on main; (3) e2e INCOMPLETE — agent reports '5 of 6 verified' with explicit statement 'Assertion 6 is not a failed fix — the fix isn't running' and 'The connector executing feed 442 is running code **over five weeks old** — long predating my change.' Agent concludes: 'Unverified that the Calendar fix works in prod' and lists owed items including 'trace how built-in connector images are built and deployed (this is the blocker); re-run assertion 6 once that lands.' The final assertion blocking e2e completion is acknowledged as unverifiable under current deployment conditions, not completed.