<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  The tested capability-backed ordering works, but compatibility-only historical rehydration still lacks a restore key, and unrelated host-context updates can repeatedly invoke snapshot persistence or restoration. These issues can leave supported ChatGPT cards blank and generate unnecessary database traffic.

Full review comments:

- [P1] Retain snapshot capabilities from compatibility metadata — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:378-383
  When a historical ChatGPT card is recreated only from `window.openai`—a supported path because `openai-compat.ts` notes that the standard `toolresult` may have occurred in an earlier page lifetime—an empty `toolOutput` can still carry the snapshot token in `toolResponseMetadata.mcp_tool_result._meta`. This code treats the data as missing but never copies that token into `snapshotCapability` because only `onToolResult` does so; with absent `toolInfo`, `restoreSnapshot()` has no lookup key and the card remains blank. Retain the snapshot capability from initial and subsequent compatibility globals as well.

- [P2] Gate snapshot RPCs on identity or state changes — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:370-371
  When a live result has valid `toolInfo`, every `hostcontextchanged` notification—including theme or container-dimension changes—calls `save_lobu_app_state` again because `hasLiveToolOutput` remains true even though identity and view state are unchanged. While data is absent, the adjacent unconditional restore similarly re-queries after each previous not-found attempt. Resizing or other context updates can therefore generate repeated database reads and writes; invoke these operations only when identity or pending state changes and memoize completed restore attempts.
  </results>
  </user_action>