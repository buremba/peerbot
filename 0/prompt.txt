also do we put the relevant context and the user can also access the web ui to talk the agent if they want and see all the conversations across everywhere? if the user doesn't have slack we should not force to, we should just list what platforms we support and let the user use sdk, web ui, telegram slack or everywhere. in betweent he agent can explai the concepts so the user learns.