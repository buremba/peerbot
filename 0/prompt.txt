Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: Production e2e verification of the feature working properly is incomplete. The transcript shows: (1) Local e2e COMPLETED — 4/4 PASS under real sandbox with no bypass flags. (2) Merge COMPLETED — PR #2243 merged at 932ccf51c on origin/main. (3) Production e2e INCOMPLETE — The assistant explicitly states 'Prod `gh` execution: **blocked by cluster runtime configuration**' and 'There is no configuration I can reach from Kubernetes that makes bwrap work here.' The assistant verified security boundaries, safe-default behavior (127 exit), and that the image contains correct binaries (`gh 2.23.0`, `git`, `bubblewrap 0.8.0`), but did NOT achieve end-to-end functional verification with `gh` executing successfully in production. The condition requires 'do e2e in prod later make sure all works properly' — the phrase 'all works properly' requires demonstrating the feature functioning end-to-end, not just that safety controls work or that failures are due to infrastructure constraints. The assistant concluded: 'Not delivered: `gh` executing in prod' and deferred the next step to the user as an infrastructure decision (installing a runtime handler, enabling userns-remap, or accepting 127 in prod).