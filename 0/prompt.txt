CONTEXT: You are taking over feature work on the lobu monorepo (access-model for agents). Work in the EXISTING worktree /Users/burakemre/Code/lobu/.claude/worktrees/agent-mutate-access (branch feat/agent-mutate-access, committed + pushed, clean tree — safe to reuse; do NOT write to ~/Code/lobu directly). First read root AGENTS.md and packages/server/AGENTS.md in the worktree.

DECISION BRIEF: /tmp/lobu-handoff/agent-access-model.md — diagnosis is verified correct. Decisions settled: (1) agents are ORG-LEVEL objects (members may use by default); (2) axis = role tier as default + extend write_approval_policies to human principals for per-agent exceptions (that extension is DB/sign-off-gated — surface the proposal, do not implement without explicit confirmation); (3) verbs = use/chat, read config, manage (update/delete/rebind); (4) read config stays org-wide; (5) REST agent mutations must be owner/admin (DONE — see below); (6) multi-org ownership: resolve by x-lobu-org slug instead of declining (orgs.length!==1 in packages/server/src/gateway/routes/shared/agent-ownership.ts:90); (7) Owletto composer must not render for a caller the server will 403.

ALREADY DONE (do not redo; verify then build on it): commit f3e0aed52 on feat/agent-mutate-access gated PATCH/DELETE /agents/:id and PATCH /:agentId/config to owner/admin via requireManageAgentAccess (agent-routes.ts), with test agent-routes-manage-access.test.ts (7/7 green, make pre-pr clean). The worktree has packages built once (make build-packages was run).

REMAINING WORK, in order:
1. PR for the #5 fix: gh pr create (fill .github/pull_request_template.md, conventional commit title), then make review once and gh pr merge when green.
2. #1/#2a/#3/#4: member-use gate on Surface B — allow org members (role tier: member→use/chat, owner/admin→manage) in packages/server/src/gateway/routes/public/agent.ts authorizeAgentAccess/requireAgentOwnership (:445). Keep it tenant-safe: never leak cross-org; reuse existing primitives (x-lobu-org resolution at :725, agentExistsInOrg, getWorkspaceRole). Separate use from read. Repro pair from the brief (owner 201 vs member 403) is the acceptance test — use the impersonation recipe in the brief for prod evidence, else a throwaway org.
3. #6: multi-org ownership by org slug instead of the orgs.length!==1 decline.
4. #7: Owletto composer gate — server exposes effective access; submodule change follows the make bump flow (push owletto first).
5. #2b: PROPOSE ONLY — human-principal kind in write_approval_policies (PolicyPrincipalKind/entity-policy.ts) for per-agent exceptions; requires sign-off before any schema/API change.

RULES: stage by explicit path; never -A; after commit verify git diff --name-only origin/main...HEAD equals intended files; run make pre-pr before pushing; evidence not assertion (show git status + diff --stat and red→green outputs in every report); one branch one concern; do not delete prod data; events is append-only; never resolve conflicts in the GitHub web UI; prefer bun. Report progress via file markers in the worktree (e.g. PROGRESS.md you append to) since herdr panes have no message bus. End every report with a confidence score per surface touched and 2-4 concrete next actions.