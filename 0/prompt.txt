<task-notification>
<task-id>wpu6clhb0</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-burakemre-Code-lobu/1680adf3-6dda-4ba4-8464-05948b9cc106/tasks/wpu6clhb0.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Root-cause the Vercel sandbox exec 429 and enumerate agent-tooling gaps for consolidation" completed</summary>
<result>{"confirmedCount":11,"totalRaised":26,"synthesis":"Every claim is now verified against source. Here is the consolidated audit.\n\n## 1. ROOT CAUSE of the 429-on-echo\n\n`vercel.ts:302` runs `await sandbox.fs.mkdir(REMOTE_WORKSPACE_DIR, { recursive: true })` unconditionally before every command. The Vercel SDK implements the recursive branch as a **command execution**, not a metadata call — `node_modules/@vercel/sandbox/dist/filesystem.js:194-200` does `await this.sandbox.runCommand(\"mkdir\", [\"-p\", path])`. Because `{recursive:true}` is hardcoded, every trivial `echo` costs **two** sandbox command-API executions instead of one, on a directory (`/vercel/sandbox`, line 11) that is the sandbox's own root and always already exists. That doubled call rate is what trips Vercel's limit. The 429 then escapes the SDK's own retry rather than being absorbed: `with-retry.js` is configured `{retries:5, maxRetryAfter:20}` and contains `if (retryAfter &gt; retryOpts.maxRetryAfter) return response;` — a sustained rate limit sends `Retry-After` &gt; 20s, so the 429 is returned **unretried**, and the post-exhaustion path `if (error instanceof APIError) return error.response;` does the same. Nothing in `vercel.ts` catches it, so an `APIError` whose message is literally `Status code ${response.status} is not ok` (`base-client.js:96`) reaches `runtime.ts:112-119`, where `errorStatus()` (`workspace.ts:10-20`) recognises only four hardcoded workspace-validation strings and buckets everything else as **500**, flattening it to `{error: \"Status code 429 is not ok\"}`. `generic-runtime-bash.ts:82-88` then writes that string into the command's own **stdout** via `onData()` and returns **exitCode 1** — the exact signal a genuinely failing command emits. The agent is told its `echo` failed, so it rewrites a perfectly good command and retries into an already-throttled endpoint.\n\n## 2. Ranked gaps (duplicates merged)\n\n**B1 — Redundant `mkdir` doubles command-API pressure.** `vercel.ts:302`. *Fix: delete the call.* `/vercel/sandbox` is the sandbox root and always exists; if a nested workspace dir is ever needed, fold `mkdir -p` into the bash command itself, which costs zero extra executions.\n\n**B2 — Infrastructure failure is indistinguishable from command failure.** Merges the three separate error-path findings (`runtime.ts:112`, `runtime.ts:117`, `generic-runtime-bash.ts:82`) — one defect spanning three layers. *Fix: type the failure at the provider, preserve it through the route, and surface it as a tool error rather than stdout+exit 1.*\n\n**B3 — Behavior runs get a brand-new sandbox every run.** `vercel.ts:115`. `conversationId` is the only per-turn-varying hash input, and Behaviors use `{agentId}_watcher_{id}_run_{runId}`, so `runId` is baked into the sandbox name. *Fix: strip the `_run_{runId}` suffix from the hash input so a Behavior's sandbox is stable across runs* — deliberate, since `sandbox-store.ts:213-218` currently treats watcher runs as ephemeral by design.\n\n**B4 — `getOrCreate` silently wipes on `snapshot_not_found`.** `vercel.ts:215`; contract at `sandbox.d.ts:565-568`. Same name, `persistent===true`, empty filesystem, no error. *Fix: pass `onCreate` and record that the sandbox was freshly provisioned in `result.meta`, so a wipe is observable and can be told to the agent.*\n\n**M1 — Lease env var name is free-form in the contract but hardcoded to `GH_TOKEN`.** `worker-env-keys.ts:63`. Any other declared name mints, audits (`credential-lease.ts:212`), then is silently stripped. Note the existing `LOBU_AGENT_ENV_` prefix already in `WORKER_ENV_ALLOWLIST_PREFIXES` — *fix: mint leases under that prefix, or derive the allowlist from resolved tooling declarations, rather than widening a static list per provider.* The fail-closed posture is right; the contract's promise of generality is what's wrong. Gates the **second** lease provider, not the first.\n\n**M2 — No capability manifest; connector tooling appears in no system prompt.** `session-context.ts:159`. `buildMcpInstructions` is the built precedent sitting next to the gap. Worse, `LOBU_DEFAULT_IDENTITY` (`session-runner.ts:1163`) asserts \"The specific connectors available to you are listed in your runtime context\" — untrue for agent tooling. *Fix: a `buildToolingInstructions` beside the MCP one, fed by the same resolved contribution.* **This is the direct cause of the fabricated \"simulated pull request\"** — an agent that is never told what it has cannot know it lacks it.\n\n**m1 — Payload-fold union copy-pasted.** `message-consumer.ts:1031` ≡ `deployment-manager.ts:1640-1668`, kept in lockstep for a non-obvious reason (`syncNetworkConfigGrants` derives binary-cache hosts from `nixConfig.packages`, `deployment-manager.ts:1226`).\n\n**m2 — `nixPackages` unioned from four sources across two files.** `platform-helpers.ts:96` documents \"three places\" and is already stale by one.\n\n## 3. Consolidation: one resolver + one typed failure\n\n**Change A — `resolveAgentTooling()` in `packages/server/src/gateway/services/agent-tooling.ts` (new).** One function taking `(agent, skills, connectorTooling, requestNixConfig)` and returning one `ResolvedTooling { nixPackages, env, domains, leases }`. It becomes the single producer for the deployment payload, the network-config grants, and the manifest.\n- **Subsumes** m1, m2, M1, M2 — four gaps, one seam.\n- **DELETE:** the fold block at `message-consumer.ts:1031`; its twin at `deployment-manager.ts:1640-1668`; the three-source union at `platform-helpers.ts:96` and its now-wrong comment. Two hand-maintained copies of a subtle invariant collapse to one call site.\n- **ADD:** `buildToolingInstructions(resolved)` in `session-context.ts`, modelled directly on `buildMcpInstructions` — same shape, same per-item reason lines. This is the manifest the user asked for: connectors *tell* the agent what it can use. It is not a side-channel; it is the same rendering path MCP already ships.\n\n**Change B — typed provider failure.** Introduce `RuntimeInfrastructureError` in `gateway/runtime/types.ts`. `vercel.ts` wraps SDK `APIError` in it; `runtime.ts` returns `{error, kind:\"infrastructure\", retryable:true}` with the true upstream status instead of a flattened 500; `generic-runtime-bash.ts` branches on `kind` and returns a tool-level error rather than `onData(...) + exitCode 1`.\n- **Subsumes** B2 entirely and makes B4 reportable.\n- **DELETE:** the string-matching `errorStatus()` in `workspace.ts:10-20` — replaced by errors that carry their own status, removing the four hardcoded magic strings.\n- Pairs with **B1** (delete the `mkdir`), which halves the call rate that produces the 429 in the first place.\n\nOrder: **B1 first** (one-line delete, immediately halves load), then **Change B**, then **Change A**.\n\n## 4. What should NOT be built\n\n**Do not add a retry/backoff wrapper around `runCommand`.** The SDK already retries with `{retries:5, factor:6}`; a second layer multiplies attempts against a throttled endpoint and makes B1's doubled call rate worse. Fix the call volume, not the symptom.\n\n**Do not add a `degraded[]` side-channel** — already rejected, and Change A's manifest makes it unnecessary: the agent is told capabilities up front instead of inferring absence from failures.\n\n**Do not add `GITLAB_TOKEN` (or any per-provider name) to `WORKER_ENV_ALLOWLIST`.** That is the instance, not the class; the list grows unboundedly and fails open by accretion. Use the existing `LOBU_AGENT_ENV_` prefix seam.\n\n**Do not build agent self-provisioning of nix packages.** This is the important one, and it is architectural, not a missing feature. `deployment-manager.ts:2255-2273` provisions nix by wrapping the **host worker process** in `nix-shell -p`. The Vercel sandbox is a **separate remote microVM** reached over HTTP at `/internal/runtime/exec` — the host's `nix-shell` wrapper cannot reach it, which is exactly why `/nix` does not exist there and `cache.nixos.org` is DNS-blocke
... (truncated 2053 chars, full result in /private/tmp/claude-501/-Users-burakemre-Code-lobu/1680adf3-6dda-4ba4-8464-05948b9cc106/tasks/wpu6clhb0.output)</result>
<diagnostics>Per-agent results: /Users/burakemre/.claude/projects/-Users-burakemre-Code-lobu/1680adf3-6dda-4ba4-8464-05948b9cc106/subagents/workflows/wf_7f6fad60-1ff/journal.jsonl — one {"type":"result",...} line per completed agent with its full return value.
If the result above is empty or unexpected, Read this file BEFORE diagnosing — do not assume agents returned non-empty results.
To re-run with edited post-processing: Workflow({scriptPath: '/Users/burakemre/.claude/projects/-Users-burakemre-Code-lobu--claude-worktrees-nix-packages-allowlist/1680adf3-6dda-4ba4-8464-05948b9cc106/workflows/scripts/sandbox-exec-gap-audit-wf_7f6fad60-1ff.js', resumeFromRunId: 'wf_7f6fad60-1ff'}) — agents whose (prompt, opts) are unchanged replay from cache.</diagnostics>
<usage><agent_count>31</agent_count><agents_done>31</agents_done><agents_error>0</agents_error><agents_skipped>0</agents_skipped><agents_empty_result>0</agents_empty_result><subagent_tokens>1691443</subagent_tokens><tool_uses>442</tool_uses><duration_ms>403442</duration_ms></usage>
</task-notification>