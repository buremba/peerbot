Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: The condition requires 'review fix issues do e2e locally and merge and do e2e in prod later make sure all works properly'. Transcript evidence shows: (1) Review issues fixed — DONE, with blocker dispositions across multiple rounds. (2) Local e2e — DONE, 4/4 PASS under a real sandbox with no bypass flags. (3) Merge — DONE, PR #2243 merged at 932ccf51c on origin/main. (4) Prod e2e verification of 'all works properly' — INCOMPLETE. The assistant verified security boundaries, the safe default (127 exit code), and image contents, but explicitly states 'Prod `gh` execution: **Blocked** — cluster runtime' and 'There is no configuration I can reach from Kubernetes that makes bwrap work here — it needs a node-level change.' The phrase 'make sure all works properly' requires end-to-end functional demonstration (gh executing successfully in production), not just verification that safety controls work or that failure is due to infrastructure constraints. The assistant concluded: 'Your next step, whichever you prefer: install crun/gvisor on the nodes... or accept 127 in prod.' This defers the final verification step to the user, leaving production e2e incomplete.