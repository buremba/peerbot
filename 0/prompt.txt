Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: Production e2e verification of the feature 'working properly' is incomplete. The condition specifies 'do e2e in prod later make sure all works properly'. Transcript evidence shows: (1) Review issues fixed — DONE, across nine rounds with mutation evidence. (2) Local e2e — DONE, 4/4 PASS under a real sandbox with no bypass flags. (3) Merge — DONE, PR #2243 merged at 932ccf51c on origin/main. (4) Prod e2e working properly — INCOMPLETE. The assistant explicitly states: 'Prod `gh` execution: **Blocked** — cluster runtime configuration' and 'There is no configuration I can reach from Kubernetes that makes bwrap work here — it needs a node-level change.' The assistant verified security boundaries, the safe-default behavior (127 exit), and image contents, but did NOT achieve end-to-end functional verification with `gh` executing successfully in production. The phrase 'make sure all works properly' requires demonstrating the feature functioning end-to-end (gh running and performing its intended function), not just verification that safety controls work or that failures are due to external infrastructure constraints. The assistant concluded: 'Blocked: `gh` executing in prod — cluster runtime configuration' and deferred the remaining verification to the user with three infrastructure options: 'install a handler, enable userns-remap, or accept 127 for now.' The production e2e is incomplete; only three of four sequential steps have been satisfied.