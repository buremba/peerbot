isn't state machine a workaround? if we have the guardrails/permissions properly can't we figure that out? we want to make it agentic as much as possible. efatura kullaniyolar. for the dynamic attributes for calculation and stuff, i thought we have derived entities mainly for this usecase. also we need to consolidate that audit logs and write it to events, don't workaround or shortcut it needs to work, interview me if needed. for date/datetime check if we use and build the ui properly. for the hooks, explain how it works, can it be code people can trigger? now next step you need to figure things out, explain all the gaps, so we can run teammate to solve them usign codex cli so you can manage. and before we start the work, for s6 and others, run fable subagent to review all the plan and use english from now on for everything for entity types to html artifact. no more turkish anymore