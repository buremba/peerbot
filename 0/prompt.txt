Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: The condition requires both local e2e and production e2e to be 'completed and verified as working properly' before merging. Transcript evidence shows: (1) Local e2e — COMPLETED, 4/4 PASS under real sandbox with no bypass flags. (2) Merge — COMPLETED, PR #2243 merged at commit 932ccf51c on origin/main. (3) Production e2e — PARTIALLY COMPLETED but NOT VERIFIED WORKING. The assistant ran tests against the production cluster confirming sandbox unavailability and security boundaries, but explicitly states the key verification is missing: 'What the prod e2e cannot yet show: `gh` running... The deployed image predates the merge by hours and has no `gh`/`git`/`bwrap`.' The assistant concludes: 'the remaining sequence is: deploy an image from this merge → enable bwrap via one of the two documented paths → re-run the probe → drive `gh` with a real installation token... Steps 2–4 are an operator decision about a security posture, not a code change.' So production e2e verification of the feature actually working (gh running) has not been completed — only the safety/containment measurements have been verified. The condition specifies e2e must be 'working properly', which requires demonstrating the feature functions end-to-end in production, not just that it safely fails to run due to missing image binaries.