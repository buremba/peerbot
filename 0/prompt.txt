Correct the UI regression before continuing: preserve the source-specific untitled fallback already shipped (`ChatGPT conversation`, `Claude conversation`, generic `MCP conversation`). Do not replace an untitled conversation title with the last tool name. Put the tool name in metadata instead, so an untitled running row reads like `ChatGPT conversation` then `Running · 1 call · Query SQL · … ago`. Update the frontend red/green test accordingly. Then finish the migration lint fixes, focused suites, broader gates, and continue the Slack-link track. Also verify the migration is rerunnable/idempotent.