Do not update or push PR #2464 to current Owletto main yet. Current Owletto main still contains the incompatible durable outputs UI from #701/#703/#705/#707; parent Lobu rejects `outputs`, which is exactly why draft PR #704 reverts that chain. A pointer to current main would ship the regression. Restore the #2464 worktree to its prior clean state/pointer and leave it blocked until #704 is reviewed and merged; then bump to the merged compatible Owletto main. Also check whether merged #2458's squash is missing from current main alongside #2457, since both are in this session's scope. Continue with read-only tracing and cleanup; do not push or merge blocked branches.