<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  The rehydration path works for populated snapshots, but it cannot process explicit clearing updates. That can leave stale rendered data or approval credentials after ChatGPT changes the compatibility globals.

Review comment:

- [P2] Honor explicit compatibility-global clears — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-chatgpt-rehydrate/packages/owletto/src/mcp-apps/_shared/host.tsx:125-127
  When ChatGPT emits a partial `openai:set_globals` update containing `toolOutput: null` or metadata without an approval capability, both parsed values are `null` and these guards preserve the previous card and token. This can attach a stale capability to a later `resolve_lobu_approval` call; distinguish omitted keys from explicit empty values and clear the corresponding state when the key is present.
  </results>
  </user_action>