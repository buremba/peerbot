<task-notification>
<task-id>w7an2n6ff</task-id>
<tool-use-id>toolu_01V9dEMF1X6V42RhRe4Q9Cc6</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-burakemre-Code-lobu/f788a6c1-21e9-429d-a821-45098b92a603/tasks/w7an2n6ff.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Mine 8GB of Claude/Codex/Grok transcripts + the lobu repo for recurring failure modes, token waste, owed work, and prod-readiness gaps" completed</summary>
<result>{"report":"The three to do first are at the top of section 1 and section 2 — they are the only ones that are both cheap and currently causing wrong outcomes.\n\n## Do these three first\n\n**1. Fix the inert-guard class, starting with the live instance.** `packages/server/src/auth/__tests__/access-model-cross-check.test.ts:130` has `if (runtimeTier === null) continue;`. Instrumenting the committed file and running `bunx vitest run` gives `{\"noTool\":41,\"manage\":9,\"silentlySkipped\":25,\"total\":128}` with `Tests 3 passed` — 75 of 128 SDK methods are never asserted while CI reports the authz drift guard green. Fix: replace the silent skip with an enumerated allowlist snapshot of the 25 currently-skipped methods so any *new* skip fails the build. Do not assert `silentlySkipped === 0`; the file's own comment documents a legitimate handler-`readOnly` fallback the static test can't reproduce, so a bare zero fails on day one. Then add to AGENTS.md: a guard with a `continue`/early-return skip path must assert its own coverage count, and a mutation test must be verified to have *landed* (re-read the file, confirm red) — `feedback_mutation_test_new_tests.md`'s `perl -0pi -e` recipe silently no-ops on whitespace mismatch, which is exactly how session 81ba9cd8 was fooled into \"passing\" a mutation that was never applied. Same class in five other subsystems (566d20f5 reconcile, d905228d approval, 54ce9add connector ACL, 50c81846 null no-op). **Highest value, ~1 hour, and it is wrong right now.** Confidence: high — reproduced live on origin/main.\n\n**2. Add the \"absence is not proven by grep\" rule to AGENTS.md.** Right now `~/Code/lobu` is on `feat/activity-memory-split` and local `main` is 14 commits behind `origin/main`, so any plain grep there reads a feature branch. Seven sessions made a wrong call from this; 59170af4 (2026-07-28) drove a wrong orphaned-script deletion off a \"zero callers\" grep against a checkout 20 commits stale. Fix, under \"Agent workflow\": *Absence is never proven by `grep`/`find` on a working tree. To prove code does not exist, run `git fetch -q origin &amp;&amp; git grep &lt;pattern&gt; origin/main` — the fetch is load-bearing since `origin/main` itself goes stale. Applies to every existence check, including the deletion-evidence rule.* Put it in AGENTS.md, not global CLAUDE.md: several incidents were in dispatched subagents that never see the global file. **Five minutes, prevents a whole failure mode.** Confidence: high.\n\n**3. Extend the screenshot hook to cover `Read`.** Across 1209 transcripts, `Read` produced 67.1 MB of base64 image data over 239 results (274 KB avg) vs `mcp__claude-in-chrome__computer` at 77.7 MB over 1058 (71.8 KB avg) — 4x the per-image cost because the MCP tool downscales and `Read` doesn't. The regime change is the tell: before 2026-07-15 (when `prefer-dom-over-screenshot.sh` shipped) `computer` was 90.7% of image bytes and `Read` 5.3%; after, `Read` is 83.6% and `computer` 9.3%. The guard worked and the cost migrated to the uncovered path. `~/.claude/settings.json` matches PreToolUse only on `mcp__claude-in-chrome__computer|browser_batch`, never `Read`. Fix: extend the same hook to match `Read`, inspect `file_path`, and emit a non-blocking nudge for `.png/.jpg` over ~150 KB pointing at `sips -Z 900 &lt;in&gt; --out &lt;scratchpad&gt;/small.png`. This directly supersedes `feedback_session_context_bloat_habits.md`, which ranked `Read` at 9% and \"negligible\" — now inverted. **Honest caveat on the cost framing:** the damage is transcript bloat and compaction pressure (a 23 MB session file), not token billing — a 3456x1730 PNG is ~1.6k tokens after Anthropic's downscale, not the ~170k a naive text heuristic suggests. Also 100 of the 165 oversized reads are one landing-repo video-frame session where the pixels *are* the deliverable; exempt that. Confidence: high on the measurement, medium on the size of the win.\n\n---\n\n## 1. Bug-free / prod-ready\n\n**Org-scoping on composite-PK tables is fixed one sibling at a time.** `agents_pkey PRIMARY KEY (organization_id, id)` (baseline.sql; same for `agent_users`, `grants`, `agent_secrets`, `conversations`), so `lobu-builder` shares one id across ~20 orgs. Eight merged PRs over three months in disjoint files: #1771, #1772, #1779, #1783, #1784, #1848, #2176 (\"Recent listed the wrong org's threads\"), and #2181 — whose body literally opens \"Problem (sibling of #2176)\", i.e. the grep-the-class rule failing after it existed. Fix: an AGENTS.md hard invariant — *ids on org-composite-PK tables are unique only within an org; every read and every delete keyed on such an id must carry `organization_id`; an org-less fallback branch or an ownership-first resolve is a cross-tenant leak, not a convenience* — enforced by making `organizationId` a **required parameter** at the resolver seam, as #1848 and #1784 each did by hand. Two corrections worth carrying: #1848's cross-tenant DELETE was **not** a live primitive (your own `project_slack_workspace_fencing_prs.md:28` records it as largely a false alarm — the fenced handler was dead code), and the tempting SQL-regex CI guard in `check-security-patterns.sh` would **not** work — the leaks lived in ambient org resolution and TS signatures, not tagged templates, and 44 non-test `agent_id` matches would fire as false positives. Confidence: high on the pattern, high on the guard being the wrong tool.\n\n**The inert-guard class** (item 1 above) also belongs here — it is the only finding where CI is actively certifying a broken state.\n\n## 2. Token &amp; wall-time efficiency\n\n**`Read` of full-res PNGs** — see item 3.\n\n**Stop requesting `statusCheckRollup`.** `gh pr view --json` is 761 KB / 325 calls / 40 sessions, the largest single sink among status commands; 492 KB of that is specifically the `statusCheckRollup` calls, which return the full per-check array when the only question is \"are required checks green\". Measured live on PR 2280: rollup form 7520 bytes vs `gh pr view 2280 --json number,title,isDraft,mergeStateStatus,reviewDecision` (182 B) + `gh pr checks 2280 --required` (679 B) = 861 bytes, 8.7x smaller and *more* legible (one line per required check, pi-review verdict inline). Fix: one AGENTS.md line under \"Session efficiency\", plus reconcile `memory/project_make_review_verdict_reality.md:24`, which currently says \"ALWAYS `gh pr checks`/`statusCheckRollup` before merging\" and so actively points at the expensive form. Honest scoping: this is ~3.6% of the 21.2 MB tool-result corpus — it leads its peer group, not the corpus. Confidence: high on the numbers, medium on the payoff.\n\n**Save CI logs to a file once.** 21 sessions, 170 redundant `gh run view … --log*` fetches on ids already pulled that session. Worst: ab87ec80 hit job 86876744628 nine times in 105 seconds, ending with `--log 2&gt;&amp;1 | sed -n '12510,12540p'` — a full remote re-download of a 12.5k-line log to read 30 lines by offset. 76bd69b4 is the textbook case: five calls on job 87202552597 over 2m23s with `grep -a` appearing only on the 4th try, proving the encoding was never inspected. Still happening 2026-07-28. Nothing in AGENTS.md, GOTCHAS.md (no CI-triage section at all), or memory covers it. Fix — new \"CI triage\" section in docs/GOTCHAS.md: *fetch once to a file (`gh run view --job &lt;id&gt; --log &gt; /tmp/ci-&lt;id&gt;.log`), use `--log` not `--log-failed` (the latter is empty for non-test job failures like check-drift and publish-packages, which is what forces the second fetch), grep needs `-a` because logs are timestamp+ANSI prefixed, strip with `sed 's/^[^Z]*Z //'` once.* Confidence: high.\n\n## 3. Owed work worth finishing\n\n**Four programs are sitting on unverified prod.** `project_mcp_client_activity_surface.md:184` (2026-07-28, program marked COMPLETE), `project_prod_mcp_reliability_sweep.md:12` (2026-07-15, PRs #1980–#1985), `project_approval_live_state_interaction_contract.md:44`, `project_multireplica_interaction_card_l
... (truncated 4011 chars, full result in /private/tmp/claude-501/-Users-burakemre-Code-lobu/f788a6c1-21e9-429d-a821-45098b92a603/tasks/w7an2n6ff.output)</result>
<diagnostics>Per-agent results: /Users/burakemre/.claude/projects/-Users-burakemre-Code-lobu/f788a6c1-21e9-429d-a821-45098b92a603/subagents/workflows/wf_84a6b5d0-87b/journal.jsonl — one {"type":"result",...} line per completed agent with its full return value.
If the result above is empty or unexpected, Read this file BEFORE diagnosing — do not assume agents returned non-empty results.
To re-run with edited post-processing: Workflow({scriptPath: '/Users/burakemre/.claude/projects/-Users-burakemre-Code-lobu/f788a6c1-21e9-429d-a821-45098b92a603/workflows/scripts/agent-corpus-mining-wf_84a6b5d0-87b.js', resumeFromRunId: 'wf_84a6b5d0-87b'}) — agents whose (prompt, opts) are unchanged replay from cache.</diagnostics>
<usage><agent_count>33</agent_count><agents_done>33</agents_done><agents_error>0</agents_error><agents_skipped>0</agents_skipped><agents_empty_result>0</agents_empty_result><subagent_tokens>2249234</subagent_tokens><tool_uses>842</tool_uses><duration_ms>840149</duration_ms></usage>
</task-notification>