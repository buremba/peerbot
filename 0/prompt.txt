<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  The focused tests, typecheck, and bundle build pass, but two untested compatibility sequences reproduce incorrect restoration behavior. In particular, completed app actions can revert visually after reload, and split metadata delivery remains order-dependent.

Full review comments:

- [P1] Preserve identity-bound app results across reloads — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:1177-1182
  When an app-initiated action such as `resolve_lobu_approval` replaces a card, `callTool` stores the replacement's capability only as `lobuViewSnapshot.resultIdentity` across reloads and detaches `toolCallId`. If the host still exposes the originating card's `toolInfo`, this branch restores using only that old ID, so the returned pre-action snapshot overwrites the approved result because the saved identity is never promoted to `snapshotCapability`. Restore the identity-bound snapshot first, or avoid overwriting a cache belonging to the app result.

- [P2] Correlate metadata that arrives before widget state — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:915-922
  When ChatGPT emits a metadata-only `openai:set_globals` event before its matching widget-state event, the displayed identity is still empty here, so the capability is rejected. The later widget event installs the same `resultIdentity`, but the rejected metadata is never reconsidered; without `toolInfo`, no restore RPC occurs and persisted disclosure/pagination state and authoritative expiry are not applied. Buffer or re-evaluate pending metadata when the matching widget identity appears.
  </results>
  </user_action>