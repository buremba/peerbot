<bash-input>POD=$(kubectl get pods -n summaries-prod -o name | grep lobu-app | head -1 | cut -d/ -f2); DBURL=$(kubectl exec -n summaries-prod "$POD" -- printenv
  DATABASE_URL); PW=$(echo "$DBURL" | sed -E 's|.*://[^:]+:([^@]+)@.*|\1|'); kubectl exec -n summaries-prod lobu-ai-prod-db-1 -- env PGPASSWORD="REDACTED" psql -U
  summaries -h 127.0.0.1 -d owletto -tA -F'|' -c "SELECT acl_support, freshness_state, count(*) FROM authz_source_acl_state GROUP BY 1,2 ORDER BY 3 DESC;"</bash-input>