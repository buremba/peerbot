<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  The new split-result lifecycle can render approval controls after silently discarding their bearer capability, making approve and reject actions fail in the target delivery sequence.

Review comment:

- [P1] Preserve approval capabilities across split standard results — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:721-726
  When ChatGPT delivers an approval result as an empty capability-bearing `toolresult` followed by the metadata-free full payload, this branch correctly treats both events as one card, but `onToolResult` later calls `setApprovalCapability(approvalCapabilityFromMeta(params._meta))`; the second event therefore clears the saved approval token. The approve/reject controls remain visible, yet `callTool` omits `lobu/approval-capability`, causing `resolve_lobu_approval` to be rejected. Preserve the prior approval capability when this continuation omits `_meta`, while still clearing it at actual result boundaries.
  </results>
  </user_action>