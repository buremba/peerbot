<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  Two compatibility paths can leave the persisted state stale or select an empty inner-frame state over a valid outer-frame snapshot. Both defects undermine the patch's primary reload-rehydration behavior.

Full review comments:

- [P1] Persist tool-output compatibility events — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-chatgpt-rehydrate/packages/owletto/src/mcp-apps/_shared/host.tsx:186-188
  When ChatGPT supplies a later live result only through `openai:set_globals`, `applyOpenAiGlobals` renders it but does not persist it because `persistInitialData` is true only for the initial synchronous read. The next page lifetime therefore restores the previous or empty widget snapshot; persist events containing `toolOutput` while keeping widget-state-only echoes read-only.

- [P2] Skip candidates without a usable Lobu snapshot — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-chatgpt-rehydrate/packages/owletto/src/mcp-apps/_shared/host.tsx:82-86
  In the supported same-origin nested topology, if the inner target exposes `widgetState: null` or unrelated widget state while the outer target contains the valid Lobu snapshot, this stores the inner object as `widgetStateFallback` and `??=` prevents the outer candidate from replacing it. With no live output, the provider remains empty despite the recoverable parent snapshot; only select candidates containing a valid versioned Lobu snapshot.
  </results>
  </user_action>