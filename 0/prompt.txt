Stop hook feedback:
[go review see if you can simplify things and merge when all done and do e2e]: Condition requires three sequential actions: (1) review and simplify, (2) merge when all done, (3) do e2e. Transcript evidence: (1) simplification review complete — agent states 'found nothing to cut', code is well-factored; (2) merged — PR #2298 squash commit 32ab462af confirmed on main via `git merge-base --is-ancestor`; (3) e2e INCOMPLETE — agent reports '5 of 6 verified' with explicit table showing assertion 6 as '⛔ stale stored source shadows the fix'. Agent's final statement: 'Say the word on `updateConnectorSource` and I'll run it with `validateConnectorSource` as preflight, then re-run assertion 6.' The sixth and final assertion remains unverified. Agent has root-caused it (stale org-scoped source in `connector_versions` shadows bundled image) but explicitly stops at the boundary: 'I asked twice without an answer' and 'That write is a different act from verifying a merge.' E2E verification is incomplete.