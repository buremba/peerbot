https://buremba.lobu.ai/activity this can be the / page directly no? so can get rid of the redirect that way. also make the default needs attention, show it before all activity, the user can switch to all activity explicitly, doing like /events in that case. the layout should be same. and we need to reuse components as much as possible