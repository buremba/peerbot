we could make the identity resolution even lazy if it will make things simpler, like the user asking who is in slack, and no entities so agent trigger scraping everybody on that channel etc. or when people dm slack knows them. but the agent needs to be relable. i thought we had this system already, via the connector generating the entity on person contact whatever and then we consolidate on $member and add the linking. so slack has its own entity but all consolidated on member