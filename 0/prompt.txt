does it even make sense to have search_sdk to use the ui? what's the best ux here rather than just trying to do best effort stuff? when run_sdk is running is the ui being updated properly or for one call we have the ui rendered after the call is done?