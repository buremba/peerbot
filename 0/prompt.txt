Stop hook feedback:
[finish the work merge what we want to merge and finish up everything and cleanup after]: The condition requires finishing the work, merging what should be merged, and cleaning up. Current transcript state: #2290 has 7/8 required checks green (integration still pending), pi-review is passing, but the PR has not yet been merged. The assistant explicitly states 'Waiting on `integration` before merging' and 'Once `integration` reports I'll merge, verify the change is live in main's source rather than trusting the PR state, and remove the 2.1G worktree.' The work is not finished — the merge is pending, verification is pending, and worktree cleanup is pending.