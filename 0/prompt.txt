Read /tmp/lobu-brief-5-prodma-gap-analysis.md and follow it. This is the primary remaining piece of work. It is STRICTLY READ-ONLY: you are producing a gap register, not code. You are running in the MAIN checkout (~/Code/lobu) precisely because you must not write to it — never edit a file here, never git switch, never commit. Your deliverable goes to /tmp. Read the memory files the brief lists BEFORE analysing anything; most of the work is already done and re-deriving it has burned cycles before. The single most important correction: the ERP board claims the entity write funnel has six bypass sites; it actually has ~22 across 9 categories, and numbering is ABSENT from origin/main. Any absence claim you make must be verified with 'git fetch -q origin && git grep <pattern> origin/main' — a working-tree grep proves nothing and past claims here have died on exactly that. The audience is Prodma's owner, a manufacturing domain expert: an overstated capability is far worse than an admitted gap. Finish with a 0-100 confidence score separating what you verified against origin/main from what you took from memory files on trust, 2-4 concrete next actions, and an explicit list of anything in those memory files you could NOT confirm still holds. Interview me on judgement calls rather than deciding for me.