Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: Production e2e has not verified the feature 'working properly'. The assistant explicitly states: '`gh` does not run in production, and cannot without a cluster runtime change' and 'The CLI-execution half is gated behind a cluster-level decision'. The transcript shows: (1) Local e2e completed with 4/4 PASS under a real sandbox. (2) Merge completed at 932ccf51c on origin/main. (3) Production measurements confirmed security boundaries and the safe default (127 exit), but `gh` execution was never verified — the deployed image predates the merge, the new image (`20260728-105827`) built successfully but bwrap cannot run on this cluster due to containerd-level uid_map restrictions, and the assistant concluded 'the feature is merged and correct; ... The CLI-execution half is gated behind a cluster-level decision'. The condition specifies 'do e2e in prod later make sure all works properly' — 'working properly' requires demonstrating end-to-end functionality, not just that safety controls work or that the failure is due to cluster limitations. Production e2e verification of the feature functioning is incomplete.