Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: The condition requires both local e2e and production e2e to be completed and verified as working properly before merging. Transcript evidence shows: (1) Local e2e HAS been completed — 4/4 PASS under a real sandbox with no bypass flags, testing both reachability and security containment. (2) PR #2243 HAS been merged — confirmed merged at `932ccf51c` on origin/main with feature code present. (3) However, production e2e has NOT been completed. The assistant explicitly states: 'Prod e2e — run `kubectl exec <app-pod> -- bwrap --unshare-user --ro-bind /usr /usr -- /usr/bin/true` on the deployed pod, then drive `gh` with a real installation token... I can't run the prod e2e from here — it needs cluster access I don't have.' The condition specifies production e2e must be completed and verified working properly before the merge, but here the merge occurred before prod e2e was run. Additionally, under the shipped chart defaults the feature exits 127 until bwrap is enabled, meaning the prod e2e as currently configured would fail — it requires an operator to first enable bwrap via SYS_ADMIN or a seccomp profile, per the chart documentation.