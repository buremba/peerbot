Before any further push or parent PR creation, fix these two correctness gaps that remain in the current commit: 1) tool_call_count must be nullable for migrated legacy/rejected rows, because DEFAULT 0 falsely claims historical zero calls. Begin accepted turn sets 0; refresh and complete use GREATEST(COALESCE(tool_call_count, 0), count); API emits no count when null and UI omits it; add a regression for a legacy row. 2) refresh conversation runtime immediately on every tool_use custom event, not only on the 20-second status heartbeat; add focused coverage. The parent branch was pushed prematurely, so add a follow-up fix commit and only then rerun focused red-to-green, migration replay/lint, and pre-pr. Do not create the parent PR until those corrections are green.