<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  The restoration state machine can discard the only capability available to hosts without toolInfo and can roll back user interaction performed before a delayed restore. Both issues affect the card-restoration behavior introduced by this patch.

Full review comments:

- [P2] Preserve capability across empty full-result replays — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/openai-compat.ts:64-66
  When an empty replay arrives as the supported `{ structuredContent: {} }` full-result envelope without `_meta`, this returns `null` rather than `undefined`. `applyOpenAiGlobals` treats that as an explicit clear, so a previously retained capability is lost and hosts that also omit `toolInfo` can no longer call `restore_lobu_app_result`; preserve the capability when the field itself is absent from an empty replay.

- [P2] Preserve local view edits made before restoration — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:375-377
  If preloaded compatibility data renders and the user changes pagination or disclosure state before a delayed connection/toolInfo restore begins, `viewStateRevisionAtStart` captures the already-incremented revision and these lines still overwrite the newer local choice with historical state. The subsequent persistence can then save that rollback; restored view state should only be applied when no local revision predates the restore.
  </results>
  </user_action>