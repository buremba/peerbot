Stop hook feedback:
[ok build all close issues review test e2e and merge when all done]: Condition requires 'merge when all done,' but #2299 remains open and unmerged. Assistant explicitly states: 'Merging #2299 needs either a `pi-review` pass or someone with branch-protection access' and 'merging it is a call I can't make.' While #2283, #2282, #2286, #2292, #2297, #2300 were merged, #2299 is blocked on a failing required check that the assistant cannot override. Issue #2288 remains open pending #2299's merge. The condition is not satisfied because not all PRs have been merged.