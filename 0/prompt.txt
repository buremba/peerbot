Prod-verify PR #2390 (runs.dry_run). Rollout gate: MERGE_SHA=8811a765403344a97021013baa9f866d5be1ef8f. Poll the deployed SHA from prod /health.revision, then `git merge-base --is-ancestor "$MERGE_SHA" "$DEPLOYED_SHA"` (merge commit FIRST — reversed accepts a stale deploy). If not yet deployed, reschedule ~1500s. Once live: verify runs.dry_run + dry_run_preview columns exist in prod, and confirm the manage_feeds trigger_feed schema exposes dry_run. Then write the result to memory and clean up any test data.