<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  Several event-ordering paths can leave the wrong card visible, permanently blank a new card, or carry and overwrite view state across result boundaries. These regressions affect the capability-only restoration path introduced by the patch.

Full review comments:

- [P1] Ignore widget snapshots during capability restoration — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:891-895
  When a changed snapshot capability starts restoring a reused ChatGPT iframe and a delayed `setWidgetState` echo from the previous card arrives first, this branch accepts the old widget snapshot because both authority flags were reset. That increments `dataRevision`, causing the restore guard at lines 473-477 to reject the new capability result and leave the previous card rendered. WidgetState-only data must remain non-authoritative while a result-boundary restore is pending.

- [P1] Let standard capabilities supersede an explicit clear — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:421-424
  When compatibility globals explicitly clear a card with `toolOutput: null` and the reused iframe's next card arrives as an empty standard `toolresult` plus a snapshot capability, `resultExplicitlyCleared` remains true and this guard prevents the new card from ever restoring. A standard result notification must establish a new result while late compatibility metadata for the cleared card remains blocked.

- [P2] Treat identical standard replays as distinct results — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:479-484
  When two consecutive calls produce identical structured data and the second arrives as a capability-only standard replay after an unkeyed first card, this fingerprint comparison decides that no result boundary occurred. The first card's pagination/disclosure state is consequently merged into and saved onto the second capability's snapshot; standard tool-result capabilities should form a boundary independently of payload equality.

- [P2] Retry failed hydration before saving capability state — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:1034-1040
  When capability hydration fails transiently in a host without `toolInfo` but cached widget data remains interactive, a later disclosure or pagination edit reaches this timer after the failed restore has left the in-flight map. The capability-only save then replaces the snapshot's entire `view_state` with the partial local map, erasing untouched persisted keys; require or retry successful hydration before saving.
  </results>
  </user_action>