Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: The condition requires both local e2e and production e2e to be completed and verified as working properly before merging. The transcript shows: (1) Local e2e testing was done on remote runtimes (Vercel, local sandbox) but the assistant explicitly states 'Neither touched the built-in runtime on the production Docker image, which is precisely where `gh` was exiting 127.' (2) The assistant documented that 'driving `gh` on that image is owed before this counts as proven in production — merged is not the same as verified.' (3) The PR #2243 has not been merged yet; the assistant states 'The next review round on #2243 is the one I'd merge on' (future tense). (4) No production e2e has been completed. The condition is not satisfied because e2e testing on the actual production Docker image has not been performed, and the PR remains unmerged pending that verification.