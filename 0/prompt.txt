Read /tmp/lobu-brief-4-prod-followups.md and follow it. Five independent prod follow-ups; do them in any order. Read AGENTS.md in this repo first — its gates are mandatory. Work ONLY in this worktree (/Users/burakemre/Code/lobu/.claude/worktrees/prod-followups); never write to ~/Code/lobu directly and never git switch here. Start each item READ-ONLY and diagnose before changing anything. Every prod SDK call MUST pass '-c prod' — the CLI's active context is currently a local dev runtime, and 'lobu apply/agent/memory/connector/org' accept no context flag at all, so an unpinned command silently hits the wrong runtime. Do NOT delete any prod feed, connection, or organization: surface the candidate and ask me. Two items may end in a small PR; if so, follow the full ship sequence in AGENTS.md including make review-fix before make review. Report a 0-100 confidence per item with the one-line basis — what you actually ran versus assumed — plus 2-4 concrete next actions. Interview me before any prod mutation or any decision that is genuinely mine.