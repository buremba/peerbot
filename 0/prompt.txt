Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: Production e2e verification of the feature 'working properly' is incomplete. The condition specifies four sequential steps: (1) review fix issues — DONE, with blockers disposed across nine rounds with mutation evidence; (2) do e2e locally — DONE, 4/4 PASS under a real sandbox with no bypass flags; (3) merge — DONE, PR #2243 merged at 932ccf51c on origin/main; (4) do e2e in prod later make sure all works properly — INCOMPLETE. The assistant explicitly states 'Prod `gh` execution: **Blocked** — cluster runtime configuration' and 'There is no configuration I can reach from Kubernetes that makes bwrap work here.' The assistant verified security boundaries, safe-default behavior (127 exit code), and image contents (`gh 2.23.0`, `git`, `bubblewrap 0.8.0`), but did NOT achieve end-to-end functional verification with `gh` executing successfully in production. The phrase 'make sure all works properly' requires demonstrating the feature functioning end-to-end (gh running and performing its intended purpose), not just verification that safety controls work or that failures are due to infrastructure constraints. The assistant concluded 'Blocked: `gh` executing in prod — cluster runtime configuration' and explicitly deferred to the user: 'Tell me which runtime direction you want — install a handler, enable userns-remap, or accept 127 for now — and I'll finish the prod run against it in one pass.' Only three of the four sequential steps have been satisfied.