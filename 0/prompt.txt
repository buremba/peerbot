Prod-verify PR #2663 (apply drift gate). Gate: MERGE_SHA=05adcd18a8e00f995b7c05d8a17bbb8762fe94ca. Get the deployed SHA from the prod image tag (see memory reference_deployed_prod_sha_from_image_tag.md), then run `git merge-base --is-ancestor "$MERGE_SHA" "$DEPLOYED_SHA"` (merge commit FIRST). If not yet deployed, reschedule ~1500s. Once deployed, verify: (1) migration `20260812010000_events_succeeded_deployments_idx` applied and `events_succeeded_deployments_idx` is VALID in prod (read-only query recipe in reference_prod_db_read_only_query_recipe.md — DB is `owletto`, `-h 127.0.0.1` load-bearing); (2) `GET /api/&lt;org&gt;/deployments/latest` returns 200 with a manifest baseline (or a clean empty/404 for an org with no succeeded deployment) rather than 500. Write the result back to memory.