<user_action>
  <context>User initiated a review task. Here's the full review output from reviewer model. User may select one or more comments to resolve.</context>
  <action>review</action>
  <results>
  Two reproducible result-transition paths retain stale content or leave the next card blank. Both affect the reused-iframe and missing-tool-info scenarios this patch is intended to support.

Full review comments:

- [P1] Prefer a new capability over stale host identity — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:450-453
  When an ID-restored card is followed in a reused iframe by a capability-only result without new `toolInfo`, this request still includes the old `tool_call_id` and `tool_name` alongside the new capability. `restoreMcpAppResultImpl` resolves the ID row first and only consults the capability when no ID row exists, so the old snapshot remains displayed; a subsequent automatic save can also collide with and expire the new snapshot. Clear the stale host identity or restore by capability alone when the capability may identify a new unkeyed result.

- [P2] Reset explicit-clear state when the host card changes — /Users/burakemre/Code/lobu/.claude/worktrees/mcp-app-v8/packages/owletto/src/mcp-apps/_shared/host.tsx:717-719
  After compatibility `toolOutput: null` clears a card, the current ID is nulled while `resultExplicitlyCleared` remains true. If the reused iframe then reports a different `toolInfo` ID, `previousId` is null so `switchedCards` remains false; the subsequent restore is rejected by the explicit-clear guard and the new historical card stays blank until non-empty output arrives. Treat an identity differing from the stored superseded identity as a new result before restoring it.
  </results>
  </user_action>