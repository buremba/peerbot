Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: The condition requires both local e2e and production e2e to be completed and verified as working properly before merging. Transcript evidence shows: (1) Local e2e was performed multiple times, but the assistant explicitly states 'I twice reported passing runs that used flags production doesn't set' and 'The honest current state is that **`gh` does not work under the shipped securityContext**, by design, until an operator supplies a bwrap-capable seccomp profile.' This means the local e2e runs that were reported as passing were against non-production configurations and do not reflect actual production behavior. (2) The assistant has not merged — the last statement is 'I'm not merging on my own judgment again here' and explicitly defers to the next review verdict. (3) No production e2e has been performed — the assistant states the feature 'does not work under the shipped securityContext' without a bwrap-capable profile, and this has never been tested on a deployed image. The condition is not satisfied because: the local e2e evidence is invalid (tested against wrong configs), the PR has not been merged, and production e2e remains incomplete.