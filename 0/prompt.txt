a couple of things here. these three tabs can be on the left, not fully centered. like the headers being on the left. when i open this page the default can be from templates. also other issues. why do we have sources here, don't we auto populate form what the user entered in the prompt? also what's the red text we don't need it. why is there multiple triggers do we support that?[Image #1]