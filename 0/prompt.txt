this blog psot is abit shit. https://lobu.ai/blog/self-improving-agents-are-event-sourced/ it starts with refefrrign another blog post. also mentions database triggers etc. we should explain it from mcp save memory superseced, the feed system we have with incrementality, and the behaviors that has triggers to process and continue back to the graph with append only logs or changing the entities. the entity changes can also be going to human approval, which then allows the users to make these changes in the main memory or collabprate with agents/humans. each agent run in a way is a hiearchy, we store agentid behavior id etc all in the events so we have transaction capability. it's like git but for real world events. make the blog post concise, no ai slop. useful, giving real evidence from our docs etc