in the first sentences, we can say intelligence is no longer blocker. even the latest generation models like GLM 5.2, Deepseek (find latest), or Kimi K3 is good enough for any intelligence. the bottleneck is context. most agents use filesystem chatgpt claude and use file directories and it's great for simple-usecases but filesystem doesn't provide a quarantee for schema. it's like analyst working on a single computer. if you want to scale further, like enterprises, building a data warehouse as a center of truth is the way. also link our dwh blog post here. and then explain use use persistent filesystem for agent runs but you define behaviors to subscribe to company events and mutate the entities or generate new audit logs tied to that agent session. that way you can see the audit logs across all agents, either managed or external. chatgpt can connect to lobu mcp and we store the mcp tool calls tied to that chatgpt conversation. or similarly on slack each thread is an agent run. behaviors are like mutations on your data models, human can approve or reject and give feedback to agents to continue etc.