Stop hook feedback:
[review fix issues do e2e locally and merge and do e2e in prod later make sure all works proeprly]: The condition requires both local e2e and production e2e to be 'completed and verified as working properly'. Transcript evidence shows: (1) Local e2e: COMPLETED and VERIFIED — 4/4 PASS under a real sandbox with no bypass flags, testing both reachability and security containment. (2) Merge: COMPLETED — PR #2243 merged at 932ccf51c on origin/main. (3) Production e2e: INCOMPLETE. The assistant states explicitly: 'Prod `gh` execution: **blocked by cluster runtime configuration**' and 'There is no configuration I can reach from Kubernetes that makes bwrap work here — it needs a node-level change'. The assistant verified security boundaries, the safe default (127 exit), and that the image contains correct binaries, but did NOT verify the feature 'working properly' in production. The condition specifically states 'do e2e in prod later make sure all works properly' — the phrase 'all works properly' requires end-to-end functional verification, not just measurement of failure modes. The production e2e lacks a successful `gh` execution demonstration, which is the core functionality being tested.