User clarification: the canonical live destination is https://app.lobu.ai/buremba/activity (confirmed from current owletto/main route /$owner/activity). Use that page for live Recent verification; do not use the stale builder-chat URL.
User clarification: use https://app.lobu.ai/buremba/activity for live Recent verification. Also do not directly bypass the required Claude review with Fable unless the user explicitly authorizes it; continue the other independent tracks meanwhile.
Critical scope correction: do NOT merge, modify, or depend on unrelated PR #2461 / behavior-outputs; that is another task's dirty work. Create a clean pointer-only Owletto bump PR from current origin/main if the drift gate still requires it. Do not use an independent Codex fallback or any unapproved reviewer override. Continue the Recent runtime-metadata and Slack-link tracks, and use /buremba/activity for live verification.
Critical scope correction: do NOT merge, modify, or depend on unrelated PR #2461 / behavior-outputs; that is another task's work. Create a clean pointer-only Owletto bump PR from current origin/main if check-drift still requires it. Do not use an independent Codex fallback or any unapproved reviewer override. Continue the Recent runtime-metadata and Slack-link tracks, and use https://app.lobu.ai/buremba/activity for live verification.